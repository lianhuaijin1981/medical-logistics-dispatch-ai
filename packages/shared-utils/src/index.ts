// ============================================================
// @med/shared-utils — 通用工具函数
// ============================================================

export { formatDate, formatDateTime, formatRelativeTime } from './date';
export { formatNumber, formatCurrency, formatPercent, formatDistance, formatDuration } from './format';
export { cn } from './cn';
export { generateOrderNo, generateTaskNo, generateId } from './id';
export { calculateDistance, pointInPolygon, getBoundingBox, isWithinTimeWindow } from './geo';
export { validatePhone, validateLicensePlate } from './validate';
