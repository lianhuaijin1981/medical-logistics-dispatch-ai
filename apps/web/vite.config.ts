import { defineConfig } from 'vite';
import react from '@vitejs/plugin-react';
import path from 'path';

export default defineConfig({
  plugins: [react()],
  resolve: {
    alias: {
      '@': path.resolve(__dirname, 'src'),
      '@med/shared-types': path.resolve(__dirname, '../../packages/shared-types/src'),
      '@med/shared-utils': path.resolve(__dirname, '../../packages/shared-utils/src'),
      '@med/ui-components': path.resolve(__dirname, '../../packages/ui-components/src'),
    },
  },
  server: {
    port: 3000,
    proxy: {
      '/api': {
        target: 'http://localhost:4000',
        changeOrigin: true,
      },
      '/socket.io': {
        target: 'http://localhost:4000',
        ws: true,
      },
    },
  },
});
